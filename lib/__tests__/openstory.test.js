const assert = require('assert');
const openstory = require('../index.js');

describe('openstory', function () {
  it('has a test', function () {
    assert(false, 'openstory should have a test');
  });
});
